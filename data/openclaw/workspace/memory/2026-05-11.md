- Setup Git/SSH durable validé pour le container OpenClaw : runtime en user `node`, `sudo` disponible, clé SSH à générer/stocker côté host Linux/WSL (pas sur `/mnt/c`) dans un dossier dédié monté en volume sur `/home/node/.ssh`, avec permissions `700` sur `.ssh`, `600` sur `id_ed25519`, `644` sur `id_ed25519.pub` et `known_hosts`. Ne pas copier la clé privée dans l’image Docker.
- Test de validation Git/SSH à refaire si besoin : `ssh -T git@github.com`, puis `git push origin main` depuis `drobsidian`. La config git minimale fonctionnelle côté container est `user.name=drobdi`, sans email requis dans ce setup.
- Contournement durable pour `gog`/Google Calendar : dans le runtime outillé, `XDG_CONFIG_HOME` n’est pas visible par défaut même si le service semble correctement configuré. Toujours lancer `gog` avec `XDG_CONFIG_HOME=/home/node/.openclaw` ou passer `env.XDG_CONFIG_HOME=/home/node/.openclaw`, sinon `gog` cherche à tort dans `/home/node/.config/gogcli/` au lieu de `/home/node/.openclaw/gogcli/`.
- Pendant les vacances signalées le 2026-05-05, ne pas proposer de nouvelles entreprises pendant toute la semaine concernée, puis rappeler à la fin des vacances ce lien Instagram : `https://www.instagram.com/reel/DX4tCUrscYd/?igsh=aTA0NXVsaXQwMDVu`.
- Il n’existait aucune tâche planifiée active pour le rollover de sprint du dimanche soir, malgré la procédure connue. Vérifications faites : `crontab -l` vide, `openclaw tasks list --json` sans tâche durable, rien dans `openclaw.json` ni `HEARTBEAT.md` pour ce rollover.
- Le 2026-05-11, j’ai appliqué manuellement les deux rollovers manqués (semaines du 2026-05-03 et du 2026-05-10) dans `drobsidian` : création/fermeture de W19 et W20, archivage des tâches non terminées, créations des reprises avec nouveaux `TD-XXXX`, mise à jour de `Taches.base`, sans toucher à `Taches.base.bak-2026-04-27-1132`. Commit poussé : `f252441` (`feat(sprints): catch up missed W19 and W20 rollovers`).
